public class Solution {
    public int solve(int[] A) {
        int min = A[0];
        int max = A[0];
        for(int i : A){
            min = Math.min(min,i);
            max = Math.max(max,i);
        }
        return min + max;
    }
}
